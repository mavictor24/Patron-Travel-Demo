export const metadata = {
  title: 'Patron Travel Studio',
  description: 'Project DataSet',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
